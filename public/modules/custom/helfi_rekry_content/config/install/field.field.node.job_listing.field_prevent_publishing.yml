langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_prevent_publishing
    - node.type.job_listing
id: node.job_listing.field_prevent_publishing
field_name: field_prevent_publishing
entity_type: node
bundle: job_listing
label: 'Prevent publishing'
description: 'Turn this on to prevent automatic publishing.'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
